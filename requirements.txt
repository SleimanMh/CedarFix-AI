# CedarFix Command AI — Python dependencies
# Minimum: shared library + test suite (currently active)
pydantic>=2.0

# ── Phase 1-2: EEP + IEP-1 FastAPI service stack ──────────────────────────
# fastapi>=0.111
# uvicorn[standard]>=0.29
# celery[redis]>=5.3
# redis>=5.0
# psycopg2-binary>=2.9
# python-multipart>=0.0.9

# ── Phase 3: IEP-2 dedup / IEP-3 calibrated routing ──────────────────────
# sentence-transformers>=3.0
# pgvector>=0.3
# lightgbm>=4.3
# scikit-learn>=1.5
# shap>=0.45

# ── Phase 4: IEP-4 LLM explanation + MLOps ───────────────────────────────
# openai>=1.25
# mlflow>=2.13
# prometheus-client>=0.20

# ── Phase 5: IEP-1 vision signal (CLIP ViT-B/32) ─────────────────────────
# Pillow>=10.3
# transformers>=4.41
# torch>=2.3 --index-url https://download.pytorch.org/whl/cpu
