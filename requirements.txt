# CedarFix Command AI — Python dependencies

# ── Shared library + test suite ────────────────────────────────────────────
pydantic>=2.0

# ── Phase 1-2: EEP + IEP-1 FastAPI service stack ──────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
redis>=5.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
aiosqlite>=0.20
python-multipart>=0.0.9

# ── Phase 2-3: IEP-1 embedding + IEP-2 dedup / IEP-3 calibrated routing ──
sentence-transformers>=3.0
scikit-learn>=1.5

# ── Phase 3 continued: pgvector, LightGBM, SHAP ──────────────────────────
# pgvector>=0.3
# lightgbm>=4.3
# shap>=0.45

# ── Phase 4: IEP-4 LLM explanation + MLOps ───────────────────────────────
# openai>=1.25
# mlflow>=2.13
prometheus-client>=0.20

# ── Phase 5: IEP-6 vision signal (CLIP ViT-B/32) ─────────────────────────
Pillow>=10.3
transformers>=4.41
torch>=2.3
